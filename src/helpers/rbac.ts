//import { AppStore } from "@/stores/app"

//const storeApp = AppStore()

export function verifica_permisos(permisos:any){ 
  /*if (storeApp.userInfo == undefined)
    return false
    
  for (let c=0; c < permisos.length; c++){
    for (let i=0; i < storeApp.userInfo.permisos.length; i++){
      if (storeApp.userInfo.permisos[i] == permisos[c]){
        return true
      }
    }
  }

  return false*/
  return true
}